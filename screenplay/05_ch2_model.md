# Chapter 2 — The model leaves the spreadsheet (3 min)

*Weakest visuals — carry it on outputs, not Run-alls. Record fourth.*

**Scene 1 — the model in Excel — 25s.**
[SCREEN] `SF_Model.xlsx`, three tabs.
[DO] change an input; SCR recalcs on the Model tab. Point at the
Calibration tab — "retyped in 100 files every quarter."

**Scene 2 — it becomes a governed asset — 25s. [★]**
[SCREEN] `02_register_model` (concept, brief), then cut to Catalog Explorer
→ Models → `sfm_scr_model`: version list, lineage.

**Scene 3 — the whole group at once — 15s.**
[SCREEN] `sfm_results` — 100 entities, `model_version` on every row.
[CUT] don't film the Run-all; cut to the table.

**Scene 4 — the payoff — 20s.**
[SCREEN] `sfm_impact` — group SCR +9.9%, broken down by module. One strong
number held on screen.

**Scene 5 — close — 5s.**
[CUT] app tile.

**Cut / never film:** filming `01`–`04` Run-alls in sequence — cut to
outputs only.
